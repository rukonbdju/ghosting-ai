[general]
static=yes
writeprotect=yes

[default]
; All inbound calls are routed to the AI bot Stasis application
exten => _X.,1,NoOp(Incoming call from ${CALLERID(num)} to AI bot)
 same => n,Stasis(telephony-engine)
 same => n,Hangup()
